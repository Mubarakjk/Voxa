# Production / TestFlight environment template
# Copy to EAS secrets or a local .env.production (never commit real values).
# All EXPO_PUBLIC_* vars are embedded in the client binary at build time.

# ── App environment ─────────────────────────────────────────────
EXPO_PUBLIC_APP_ENV=production

# ── Release feature gates (must stay false until features ship) ─
EXPO_PUBLIC_EXPERIMENTAL_FEATURES=false
EXPO_PUBLIC_REALTIME_VOICE_ENABLED=false
EXPO_PUBLIC_SCHEDULED_CALLS_ENABLED=false
EXPO_PUBLIC_VOICE_NOTES_ENABLED=false
EXPO_PUBLIC_MICROPHONE_CHAT_ENABLED=false

# ── Supabase (production project) ───────────────────────────────
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# ── AI — prefer gateway; never ship permanent OpenAI secret keys ─
EXPO_PUBLIC_AI_GATEWAY_URL=
# Leave empty in App Store / TestFlight builds
EXPO_PUBLIC_OPENAI_API_KEY=

# ── RevenueCat (public SDK keys only) ───────────────────────────
EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY=
EXPO_PUBLIC_REVENUECAT_ENTITLEMENT_ID=voxa_pro
EXPO_PUBLIC_REVENUECAT_OFFERING_ID=default
EXPO_PUBLIC_REVENUECAT_PRODUCT_MONTHLY=
EXPO_PUBLIC_REVENUECAT_PRODUCT_ANNUAL=
EXPO_PUBLIC_REVENUECAT_TEST_STORE=false

# ── Weather — Open-Meteo default; never use mock in production ──
EXPO_PUBLIC_WEATHER_PROVIDER=
EXPO_PUBLIC_WEATHER_API_URL=

# ── Legal (production URLs required for App Store) ──────────────
EXPO_PUBLIC_PRIVACY_POLICY_URL=https://voxa.app/privacy
EXPO_PUBLIC_TERMS_OF_SERVICE_URL=https://voxa.app/terms
EXPO_PUBLIC_SUPPORT_EMAIL=support@voxa.app

# ── Optional dev-only tokens (omit in production) ─────────────────
EXPO_PUBLIC_AUDD_API_TOKEN=
